<div class="col-3">
  <div class="card">
    <div class="card-header">
      {{survey.title}}
    </div>
    <div class="card-body">
      {{#each survey.questions as |question|}}

        <p>{{question.title}}</p>
      {{/each}}
      <a href="#" class="btn btn-primary" data-id="{{survey._id}}">Respond</a>
      <a href="#" class="btn btn-primary">Results</a>
      {{#if (ownership survey.owner) }}
        {{> delete survey = survey}}
        {{> edit survey = survey}}
      {{/if}}
    </div>
  </div>
</div>
